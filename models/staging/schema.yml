version: 2

models:
  - name: aboba_processing
    description: "A starter dbt model"
    columns:
      - name: _id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
